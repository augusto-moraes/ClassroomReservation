import * as React from 'react';
import Card from '@mui/material/Card';
import AutoGrid from './containers/Grid';



export default function BasicCard() {
  return (
    <div style={{
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      }}>
    <Card>
      hedi
    </Card>
    </div>
  );
}